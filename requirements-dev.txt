# Development dependencies. For runtime, see requirements.txt.
# Install with:  pip install -r requirements-dev.txt
# (or: pip install -e ".[dev]"  to also install the project itself)

-r requirements.txt

pytest==8.3.4
pytest-cov>=5.0
ruff>=0.8
mypy>=1.13
pre-commit>=4.0
types-lxml>=2024.12

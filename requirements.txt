# Vercel lee este archivo para resolver deps en el deploy del microservicio.
# Instala los paquetes locales en modo editable para que Vercel los empaquete
# con todas sus dependencias transitivas.
-e packages/core
-e packages/api
